// Do not delete this format, only change the code inside the function
// Use console.log, not return
module.exports.quotedText = function (input) {
    console.log("'" + input + "'"); // adds single quotation marks to both ends of the input
};