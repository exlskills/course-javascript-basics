// Do not delete this format, only change the code inside the function
// Use console.log, not return
module.exports.parseNumbers = function (input) {
    console.log(parseInt(input) + 1); // parse the string into an integer using parseInt() and adds one to the value
};