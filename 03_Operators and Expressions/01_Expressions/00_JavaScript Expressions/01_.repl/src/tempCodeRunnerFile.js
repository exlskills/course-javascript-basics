let radius = (150 - 20) / 2 + 5;

// Expression for calculation of circle area
let area = Math.PI * radius * radius;

// Expression for calculation of circle perimeter
let perimeter = 2 * Math.PI * radius;

console.log(radius);
console.log(area);
console.log(perimeter);