// Do not delete this format, only change the code inside the function
// Use console.log, not return
module.exports.checkEven = function (input) {
    console.log(input % 2 == 0); // prints true if input is divisible by 2, which is the same as having no remainder
};