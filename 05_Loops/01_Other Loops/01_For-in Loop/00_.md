`for-in` loop **iterates over the properties of an object**. When the object is **array**, **nodeList** or **liveNodeList**, `for-in` iterates over their **elements**. When the object is not a collection, `for-in` iterates over **its properties**.