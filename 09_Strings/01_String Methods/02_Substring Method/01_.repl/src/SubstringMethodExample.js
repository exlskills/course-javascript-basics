var str = "Hello World!";

console.log(str.substr(0, 5)); // starts from index 0 and returns 5 characters
console.log(str.substring(6, 12)); // starts from index 6 (inclusive) and ends at index 12 (exclusive)